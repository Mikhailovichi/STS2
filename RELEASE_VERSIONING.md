# STS2 Mod Release Versioning

本文档适用于当前仓库内全部对外发布的 mod。

## Version Format

统一使用 `x.y.z`：

- `x` 为重大架构变更
- `y` 为功能新增
- `z` 为 Bug 修复与优化

## Increment Rules

- 当 `x` 递增时，`y` 与 `z` 归零，例如：`1.4.3 -> 2.0.0`
- 当 `y` 递增时，`z` 归零，例如：`1.4.3 -> 1.5.0`
- 当 `z` 递增时，仅递增修复号，例如：`1.4.3 -> 1.4.4`
- 单次发布以本次变更中最主要的类型作为版本号递增依据

## Classification

- `x`：底层架构重构、核心依赖替换、公共接口变化、兼容性风险变化等重大调整
- `y`：新增可感知功能、新配置项、新支持场景或现有能力扩展
- `z`：缺陷修复、稳定性提升、性能优化、资源修正、文案修正等非破坏性调整

## Examples

- `1.0.0`：首个稳定公开版本
- `1.1.0`：新增一个明确功能
- `1.1.1`：修复问题并做轻量优化
- `2.0.0`：发生重大架构升级或兼容性变化

## Packaging Notes

- 所有 mod 的发布版本号统一遵循本规范
- 推荐发布包文件名格式为 `<ModName>_<x.y.z>.zip`
- 各 mod 的 `README`、发布包、变更记录应尽量保持同一版本号
